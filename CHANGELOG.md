# Changelog — سیرمان / لایق الکترونیک پارسیان

نسخه محصول شمسی است: `{Major}.{ماه}.{روز}{حرف یونانی}`. منبع: `SIRMAN_VERSION.json`.

قالب هر بند: Version, Date, Added, Changed, Fixed, Known Issues.

---

## 1405.6.3α — ۱۴۰۵/۰۶/۰۳

### Added

- کیت نصب کامل خودکفا (Runtime دات‌نت داخل App).
- چاپ بومی برچسب پستی با دکمهٔ صریح «چاپ بومی برچسب»؛ مسیر HTML قدیمی مانده است.
- چاپ بومی فاکتور و صفحه آزمایش (از مسیر قبلی همین خط محصول).

### Changed

- شماره نسخه با تاریخ شمسی امروز یکی شد (`1405.6.3α` / اسمبلی `1405.6.3.1`).
- این کیت همان کار چاپ، ذخیره رسانه روی هارد، و راهنمای ورد را یکجا دارد.
- SQLite هنوز candidate است؛ منبع زنده همان localStorage است.

### Known Issues

- چاپ فیزیکی روی PC فروشگاه هنوز تأیید نشده (`PHYSICAL_PRINT_NOT_VERIFIED`).
- دکمهٔ بومی برچسب برای آزمایش است؛ سوییچ اجباری همه کاربران نیست.

---

## 1405.6.2α — ۱۴۰۵/۰۶/۰۲

### Changed

- شماره نسخه با تاریخ شمسی امروز یکی شد (`1405.6.2α` / اسمبلی `1405.6.2.1`).
- کیت نصب فروشگاه همین نسخه دوباره بسته‌بندی شد.

### Added

- هیچ قابلیت کسب‌وکار جدیدی اضافه نشد.

### Known Issues

- چاپ فیزیکی روی PC فروشگاه هنوز تأیید نشده (`PHYSICAL_PRINT_NOT_VERIFIED`). ماژول چاپ منجمد است.
- SQLite فقط candidate است؛ منبع زنده همان localStorage است.

---

## 1405.5.27γ — ۱۴۰۵/۰۵/۲۷

### Added

- کارت فقط‌خواندنی «عملیات امروز» روی داشبورد موجود: فوری، امروز، در انتظار، انبار/مالی.
- اسنپ‌شات `getDailyOperationsBriefSnapshot` از وضعیت فعلی می‌خواند و چیزی ذخیره نمی‌کند.

### Changed

- هیچ موتور فاکتور / انبار / حساب / گارانتی / بک‌آپ / چاپ عوض نشد. رفرش همان `renderDashboard` موجود است.

### Known Issues

- چاپ فیزیکی روی PC فروشگاه هنوز تأیید نشده (`PHYSICAL_PRINT_NOT_VERIFIED`). ماژول چاپ منجمد است.

---

## 1405.5.27β — ۱۴۰۵/۰۵/۲۷

### Added

- پیش‌نمایش و تأیید صریح قبل از ادغام دریافت فضای کاری شبکه.
- خلاصهٔ افزوده می‌شود / به‌روزرسانی می‌شود / بدون تغییر / نیاز به بررسی، بدون موتور ادغام دوم.

### Changed

- دریافت از پوشه مشترک دیگر بلافاصله ادغام نمی‌کند؛ انصراف داده را عوض نمی‌کند. تأیید همان `applyBackupSelective` موجود را صدا می‌زند.

### Known Issues

- چاپ فیزیکی روی PC فروشگاه هنوز تأیید نشده (`PHYSICAL_PRINT_NOT_VERIFIED`). ماژول چاپ منجمد است.

---

## 1405.5.27α — ۱۴۰۵/۰۵/۲۷

### Added

- هارنس تشخیص سخت‌افزار چاپ جدا از مرکز پرینت (`PrintHardwareDiagnostic` + تب تنظیمات «تشخیص چاپگر»).
- مسیر چاپ مستقیم ویندوز (GDI) در برابر مسیر جداگانه WebView2 PrintAsync برای مقایسه لایه شکست.
- لاگ `PRINT_DIAGNOSTIC.log` و گزارش `PRINT_HARDWARE_DIAGNOSTIC_REPORT.md`.
- قرارداد چاپ `IPrintService` / `PrintStatusContract` به‌عنوان wrap روی موتور موجود (بازنویسی نیست).

### Changed

- هیچ منطق فاکتور / انبار / حساب / گارانتی / مرکز پرینت تولیدی عوض نشد.
- چاپ از این نسخه **ISOLATED / FROZEN** است تا ورود فاز ۳؛ جزئیات اسپولر/درایور پشت قرارداد می‌ماند.

### Known Issues

- چاپ فیزیکی روی PC فروشگاه هنوز تأیید نشده (`PHYSICAL_PRINT_NOT_VERIFIED`). این نسخه isolation است نه FIX چاپ.

---

## 1405.5.26α — ۱۴۰۵/۰۵/۲۶

### Added

- پروتکل حاکمیت توسعه: `docs/DEVELOPMENT_GOVERNANCE.md`

### Changed

- مسیر چاپ، چاپگر فایل/PDF را به‌عنوان چاپ کاغذ قبول نمی‌کند.
- خروجی PDF از دکمه جدا است و چاپگر چاپ را عوض نمی‌کند.

### Fixed

- انتخاب خودکار Microsoft Print to PDF / پیش‌فرض فایل به‌جای چاپگر واقعی.

### Known Issues

- چاپ فیزیکی روی ویندوز فروشگاه هنوز تأیید نشده (BLOCKED — PRINTER ENVIRONMENT ISSUE).
- EXE قدیمی با HTML جدید برای چاپ کافی نیست.

---

## 1405.5.25β — ۱۴۰۵/۰۵/۲۵

### Changed

- چاپ از WebView2.PrintAsync به اسپولر ویندوز؛ موفقیت جعلی printto حذف شد.

### Known Issues

- در تست فروشگاه همچنان PDF تولید می‌شد (رفع در 1405.5.26α).

---

## 1405.5.25α — ۱۴۰۵/۰۵/۲۵

### Fixed

- هویت داخلی فاکتور (`InvoiceId`)؛ حذف دیگر با شماره روی برگه رکورد دیگر را برنمی‌دارد.
- شماره تکراری در فاکتور جدید رد می‌شود.

---

## Older

تاریخچه جزئی‌تر در صفحه راهنما (تازه‌های نسخه) و گزارش‌های `deliveries/Reports/` است.
