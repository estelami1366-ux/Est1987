# ۱۴۰۵/۰۵/۲۳

- کاربر گفت آپدیت معلوم نیست کجاست و فایل آخر فقط ۱ کیلوبایت است.
- راهنمای نصب از صفر نوشته شد: `راهنمای_نصب_از_صفر.txt` و `راهنمای_نصب_و_آپدیت.docx`.
- هشدار صریح: JSON یک‌کیلوبایتی برنامه نیست؛ برنامه واقعی `Sirman_Final.html` حدود ۱٫۶ مگابایت است.
- کیت نصب کامل: `Sirman_Setup_1405.5.23ξ.zip` — Extract و فقط `نصب.bat`. بدون کپی‌پیست.
- علت نرسیدن به ξ: `Sirman_Pending_Update.json` هنوز ۱۴۰۵.۵.۲۱β بود و Start برنامه را برمی‌گرداند؛ JSON ξ هم فقط setVersion بود. هر دو با آپدیت کامل HTML عوض شدند.
- نسخه جاری: `1405.5.23ρ` / اسمبلی `1405.5.23.18`.
- باگ واقعی: حذف فاکتور/گارانتی رکورد را پاک می‌کرد ولی قطعه و مبلغ همان سند برنمی‌گشت. اصلاح شد: `invoice.delete` / `warranty.delete` در Core با برگشت از روی refId/refType و اقلام همان سند. گزارش: `FINAL_TRANSACTION_REVERSAL_FIX_REPORT.docx`.
- کاربر گفت با حذف سند همه آثار همان سند باید برگردد، حساب مالی هم همین قانون ساده. فروش هم مبلغ حساب را برمی‌گرداند؛ مالکیت مالی = شماره سند (refId)، نه نوع تراکنش.
- کاربر گفت کیت‌ها خودشان آپدیت‌اند ولی نسخه عقب می‌ماند / فایل آپدیت داخل کیت نیست. علت: کیت عمداً JSON آپدیت را نمی‌گذاشت؛ Pending قدیمی ۱ کیلوبایتی روی پوشه نصب قبلی می‌ماند. از ρ فایل آپدیت همین نسخه داخل `App/updates` و Pending هم‌نسخه است؛ بعد از نصب آپدیت جدا لازم نیست.
