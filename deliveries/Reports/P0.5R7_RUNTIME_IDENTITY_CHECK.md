# SIRMAN — P0.5R7 RUNTIME IDENTITY CHECK
## Read-only: which executable is actually running?

**Gregorian:** 31 August 2026  
**Timezone:** UTC  
**Mode:** READ ONLY — no source / print / storage / version / build change  
**Agent OS:** Linux 6.12.94+ (this environment is **not** the shop Windows PC)

Shop report (treated as observation, not as binary identity):

```text
Installed shop app behaves like the previous version.
No Diagnostic History is visible.
Print workflow appears unchanged.
```

This packet asks which **running** `Sirman.exe` that is. This Linux agent has **no process, no file lock, and no hash** of the shop machine’s binaries. Items 1–6, 10–11, 14–17 for the **running shop process** are therefore **NOT OBSERVED**.

What **is** proven here: P0.5R7 kit identity, source wiring, UI location, and why “print looks the same” is expected even on the correct R7 build.

---

## Gate

```text
Requested: read-only runtime identity check
Classification: documentation / forensic — no product code
Source files edited: NO (this report only)
Print / Storage / Backup / Version: NO
Build: NOT RUN
Gate: PASS — read-only
```

---

## Git (this worktree — not shop)

```text
Report branch:  cursor/p0-5r7-runtime-identity-fa01
Parent HEAD:    d50184c  (P0.5R7 shop-kit stamp)
P0.5R7 code:    b57c465
P0.5R7 pack:    0930917  from build HEAD 2c490da
No merge/rebase/reset/cherry-pick
No product source diff
```

---

## 1. Exact path of running Sirman.exe

```text
RUNNING (shop Windows):  NOT OBSERVED
This Linux agent:        no Sirman.exe process
```

Where the installer **would** put it (source of `install-setup.ps1`, not a live path):

```text
Folder the user picked in SETUP.bat  (default prompt: Documents\Sirman)
Shortcut:  %APPDATA%\Microsoft\Windows\Start Menu\Programs\Sirman\Sirman.lnk
Remembered dest:  %LOCALAPPDATA%\Sirman\install-location.txt
```

A leftover **old** Start-menu / Desktop shortcut can still launch a previous kit (P0.5R6 or earlier) after a new ZIP is extracted elsewhere.

Live path command (shop, not run here):

```powershell
Get-CimInstance Win32_Process -Filter "Name='Sirman.exe'" |
  Select-Object ProcessId, ExecutablePath
```

---

## 2. FileVersion of running exe

```text
RUNNING:  NOT OBSERVED
```

FileVersion is **not** a discriminator:

```text
P0.5R7 kit FileVersion:  1405.6.3.1
P0.5R6 kit FileVersion:  1405.6.3.1
Product stamp (unchanged): 1405.6.3α / assembly 1405.6.3.1
```

Windows “File version 1405.6.3.1” can be **either** kit.

---

## 3. ProductVersion of running exe

```text
RUNNING:  NOT OBSERVED
```

Discriminator (PE ProductVersion / InformationalVersion, SDK SourceRevisionId):

```text
P0.5R7 kit (must match if this pack is running):
  1405.6.3α+2c490da4135875736617e7aedc3473f3c151b00e

P0.5R6 kit (previous diagnostic pack):
  1405.6.3α+4de1a7b4ebde6d1e9cc56a3abd072a7a9477a591
```

Wrong-build stop: if shop ProductVersion does **not** contain `2c490da4135875736617e7aedc3473f3c151b00e`, the running exe is **not** the P0.5R7 diagnostic build.

---

## 4–6. SHA-256 of running binaries

```text
RUNNING Sirman.exe:       NOT OBSERVED
RUNNING Sirman.dll:       NOT OBSERVED
RUNNING Sirman.Core.dll:  NOT OBSERVED
```

P0.5R7 shop package (re-hashed this run, match = YES):

| File | Size | SHA-256 |
|---|---|---|
| `Sirman.exe` | 152064 | `eef93e832a02b4c8d94e3b3378731684d8a21610229114d382e26365d787b6a3` |
| `Sirman.dll` | 200704 | `5b818324cd230c99518febc700402f7eb54ebeff76088196b6b0e0f111f39cc7` |
| `Sirman.Core.dll` | 217600 | `1e5f91b363fdd35822d1fb1a7595baccc0487a42849327e975449ceb4e0b580f` |

P0.5R6 shop package (contrast):

| File | SHA-256 |
|---|---|
| `Sirman.exe` | `0a73ffd76f0cfae405d2e8e2f19d6b382ee051f83dec862da99a28e6bf1b8579` |
| `Sirman.dll` | `097187d026bf8336762f0e03e7b271eab8ee32fd340e165f911b7d192ac4e42a` |
| `Sirman.Core.dll` | `2488221269d4cb58ebd4141b167d44f16e3c37ba25da8f5ff66e04ba2c89aa06` |

All three R7 hashes **differ** from R6. Identity is SHA-256 + ProductVersion, not FileVersion.

---

## 7. Does **running** Sirman.dll contain probe / logo / history?

```text
RUNNING Sirman.dll strings:  NOT OBSERVED
```

P0.5R7 **kit** `App/Sirman.dll` + `App/Sirman.Core.dll` (already proven at pack time, kit files unchanged this run):

```text
NativePrintRuntimeProbe          HIT in kit Sirman.dll
P0.5R4_NATIVE_RUNTIME.log        HIT
P05R6_LOGO / stage=LOGO          HIT
NativeLogoSource                 HIT (dll + Core)
DiagnosticHistory                HIT in kit Sirman.dll
DiagnosticHistoryStore           HIT in kit Sirman.Core.dll
history.jsonl                    HIT in kit Sirman.Core.dll
DIAG_HISTORY_WRITE_FAILED        HIT in kit Sirman.dll
```

P0.5R6 kit HTML has **no** `phd-history` / `phdHistory()`. P0.5R7 kit HTML has both.

---

## 8. Exact Diagnostic History implementation path in source

```text
Core store / JSONL:
  desktop/Sirman.Core/Printing/DiagnosticHistory.cs
    DiagnosticHistoryStore
    DefaultFilePath → {appDataRoot}/diagnostics/history.jsonl
    FileName = "history.jsonl"

Desktop bridge:
  desktop/Sirman.Desktop/DiagnosticHistoryBridge.cs
    FilePath = DiagnosticHistoryStore.DefaultFilePath(AppPaths.AppDataRoot)

Native Test Page / Postal append (after Print(), not invoice):
  desktop/Sirman.Desktop/NativeWindowsPrintService.cs
    TryAppendDiagnosticHistory

Hardware diagnostic append / history actions:
  desktop/Sirman.Desktop/PrintHardwareDiagnostic.cs
    actions: "history", "historyevent"
    TryRecordJobHistory, ConfirmPaper → TryAppendVerification

Host (no new method):
  desktop/Sirman.Desktop/SirmanHostObject.cs
    RunPrintHardwareDiagnostic

AppData root:
  desktop/Sirman.Desktop/AppPaths.cs
    LocalApplicationData\Sirman
```

---

## 9. Exact runtime history path

```text
%LocalAppData%\Sirman\diagnostics\history.jsonl
```

Expanded on a typical shop profile:

```text
C:\Users\<SHOPUSER>\AppData\Local\Sirman\diagnostics\history.jsonl
```

Not `sirman.sqlite`. Not Print Center `printEngineHistory`. Not `%LocalAppData%\Sirman\print\print-jobs.jsonl`. Not `P0.5R4_NATIVE_RUNTIME.log`.

The file is created on **first successful Append**, not on application start (`Directory.CreateDirectory` + `File.AppendAllText` inside `DiagnosticHistoryStore.Append` only).

---

## 10. Does that file exist after starting the application?

```text
SHOP file after start:  NOT OBSERVED
This Linux agent:       file does not exist (no Windows LocalAppData\Sirman)
```

From source: **starting the app does not create `history.jsonl`.**  
Probe / enumerate / Print Center invoice print also do **not** create it.

It is created only after a successful append from:

- native `testPage` submit
- native `postalLabel` submit
- hardware diagnostic Direct / WebView2 submit (or fail-with-code)
- Confirm Paper / `historyEvent` verification

---

## 11. Size and last-write time of history.jsonl

```text
SHOP:  NOT OBSERVED  (file presence unknown)
```

---

## 12. How Diagnostic History is exposed in the UI

**Not** in Print Center («تاریخچه چاپ» / `printEngineHistory`). That screen is unchanged on purpose.

P0.5R7 viewer is only here:

```text
Settings → tab 🧪 تشخیص چاپگر  (showStgTab('print-diag'))
panel #stg-print-diag
box    #print-hardware-diagnostic
heading "Diagnostic History"
status  #phd-history-status
table   #phd-history-table
wrapper #phd-history
```

Until the history button is clicked, status text is:

```text
تاریخچه هنوز بارگذاری نشده. هر اجرا یک شناسه جلسه جدید می‌سازد و رکورد قبلی را پاک نمی‌کند.
```

That is an empty viewer, not proof the DLL lacks history.

Help card `data-help-id="print-hardware-diagnostic"` also documents the JSONL path.

If the running HTML is P0.5R6 or older, this whole “Diagnostic History” block is **absent**. That matches “no Diagnostic History is visible” without hashing.

HTML load order (`MainForm.ResolveHtmlPath`): CLI arg → `PreferredHtmlPath` in desktop-settings.json → `sirman_html_path.txt` beside exe → `Sirman_Final.html` beside exe. An old preferred HTML path can hide the R7 UI even if a new exe was copied.

---

## 13. Exact UI handler / button / function for View History

```text
Button label:   📋 تاریخچه تشخیص
onclick:        phdHistory()
HTML:           Sirman_Final.html  (kit App\Sirman_Final.html same)

function phdHistory(){
  var r = phdCall('history');
  phdRenderHistory(r);
  return r;
}

phdCall → chrome.webview.hostObjects.sync.sirmanHost.RunPrintHardwareDiagnostic
payload action: "history"

Host: PrintHardwareDiagnostic.Run → case "history" → HistoryJson()
```

Also auto-called (does not replace the button) after:

```text
phdDirectPrint  → setTimeout phdHistory
phdWebViewPrint → setTimeout phdHistory
phdConfirmPaper → phdHistory()
```

HTML-only / no Host: `phdCall` returns `errorCode: NO_HOST` and the status becomes “تاریخچه تشخیص فقط در Sirman.exe ذخیره می‌شود…”.

Old exe + new HTML: `action=history` hits `UNKNOWN_PRINT_FAILURE` (“عمل تشخیص ناشناخته است”).

---

## 14. Does a single diagnostic run append a record to history.jsonl?

```text
SHOP append observed:  NOT OBSERVED
```

Source contract (P0.5R7 build only):

```text
YES — one new JSONL line (new sessionId D-… ) after:
  Native Test Page Print() success
  Native Postal Label Print() success
  Hardware Direct / WebView2 submit (Submitted or Error)

YES — second line, same sessionId, after Confirm Paper
NO  — app start
NO  — printer probe / enumerate
NO  — invoice native print (production; names/addresses not stored)
NO  — Print Center history UI
```

If the running binaries are **not** P0.5R7, no append path exists.

---

## 15. If no record is written, exact failure reason

```text
SHOP write-failure log:  NOT OBSERVED
```

Possible reasons, ranked by evidence we **do** have vs guess:

| Reason | Status |
|---|---|
| Running exe/HTML is not P0.5R7 (no `phd-history`, no `history` Host action, SHA ≠ kit) | **CONSISTENT with shop symptom; not proven without hash** |
| App started / Print Center used / invoice printed — no append by design | **SOURCE FACT** |
| Viewer not opened: Settings → تشخیص چاپگر → «تاریخچه تشخیص» | **SOURCE FACT — easy miss** |
| `history.jsonl` not created until first append | **SOURCE FACT** |
| `PreferredHtmlPath` / old shortcut still on previous kit | **POSSIBLE; path not observed** |
| History write failed (`DIAG_HISTORY_WRITE_FAILED` in job log / PRINT_DIAGNOSTIC.log); print continues | **POSSIBLE; log not observed** |
| HTML-only (`NO_HOST`) | **would show explicit status text** |

There is **no** observed shop log line naming a write error. Declaring a single failure reason for the live PC would be invention.

---

## 16. Is the running executable actually the P0.5R7 diagnostic build?

```text
RUNNING IDENTITY:  NOT PROVEN
```

This agent cannot say YES or NO for the shop process.

Required YES conditions (all):

```text
ProductVersion contains 2c490da4135875736617e7aedc3473f3c151b00e
Sirman.exe SHA-256      = eef93e832a02b4c8d94e3b3378731684d8a21610229114d382e26365d787b6a3
Sirman.dll SHA-256      = 5b818324cd230c99518febc700402f7eb54ebeff76088196b6b0e0f111f39cc7
Sirman.Core.dll SHA-256 = 1e5f91b363fdd35822d1fb1a7595baccc0487a42849327e975449ceb4e0b580f
HTML contains id="phd-history" and function phdHistory
```

FileVersion `1405.6.3.1` or product `1405.6.3α` **alone** is a NO-PROOF.

---

## 17. Compare running hashes against the P0.5R7 shop package

```text
RUNNING vs package:  NOT OBSERVED (no running hashes)
```

Package vs previous pack (this agent, files on disk):

```text
Sirman.exe       P0.5R7 ≠ P0.5R6
Sirman.dll       P0.5R7 ≠ P0.5R6
Sirman.Core.dll  P0.5R7 ≠ P0.5R6
```

---

## Symptom vs design (do not treat as identity)

```text
"Print workflow appears unchanged"
  EXPECTED on a correct P0.5R7 build.
  P0.5R7 did not change DrawTestPage / DrawPostalLabel / DrawInvoicePage /
  PaperSize / logo resolver / Print Center.

"No Diagnostic History is visible"
  If looking at Print Center history: expected even on R7.
  If Settings → تشخیص چاپگر has no "Diagnostic History" heading: HTML is pre-R7
    (or old PreferredHtmlPath). That is an identity clue — still confirm with SHA.
  If the heading exists but the table is empty: click «تاریخچه تشخیص»;
    file may not exist until a Test Page / Postal / Direct diagnostic submit.
```

---

## Shop capture (not executed here)

On the Windows PC that is running Sirman, from an elevated-enough PowerShell:

```powershell
$p = Get-CimInstance Win32_Process -Filter "Name='Sirman.exe'" |
  Select-Object -First 1
$exe = $p.ExecutablePath
$dir = Split-Path $exe
$fi = [System.Diagnostics.FileVersionInfo]::GetVersionInfo($exe)
function Get-Sha([string]$path) {
  (Get-FileHash -Algorithm SHA256 -LiteralPath $path).Hash.ToLower()
}
[pscustomobject]@{
  Path            = $exe
  FileVersion     = $fi.FileVersion
  ProductVersion  = $fi.ProductVersion
  ExeSha          = Get-Sha $exe
  DllSha          = Get-Sha (Join-Path $dir 'Sirman.dll')
  CoreSha         = Get-Sha (Join-Path $dir 'Sirman.Core.dll')
  InstallLocation = Get-Content "$env:LOCALAPPDATA\Sirman\install-location.txt" -ErrorAction SilentlyContinue
  HistoryPath     = Join-Path $env:LOCALAPPDATA 'Sirman\diagnostics\history.jsonl'
} | Format-List
Get-Item (Join-Path $env:LOCALAPPDATA 'Sirman\diagnostics\history.jsonl') -ErrorAction SilentlyContinue |
  Select-Object FullName, Length, LastWriteTime
Select-String -LiteralPath (Join-Path $dir 'Sirman_Final.html') -Pattern 'phd-history','function phdHistory' |
  Select-Object -First 5
```

Compare ExeSha/DllSha/CoreSha to section 4–6. Compare ProductVersion to `2c490da4135875736617e7aedc3473f3c151b00e`.

---

## Final

```text
No source changes.
No print changes.
No storage changes.
No version changes.
No build.

Running shop Sirman.exe path / SHA / ProductVersion: NOT OBSERVED
P0.5R7 kit hashes (on disk): CONFIRMED
Print-looking-unchanged: expected even if R7 is running
History UI: Settings → تشخیص چاپگر → phdHistory() only; not Print Center
history.jsonl: not created on startup

Final status: IDENTITY INCOMPLETE — SHOP HASH REQUIRED
```

STOP — WAIT FOR REVIEW.

# END P0.5R7 RUNTIME IDENTITY CHECK
