# SIRMAN — P0.2S WINDOWS RUNTIME PRINT DIAGNOSTIC
## One controlled diagnostic — NO PRINT ENGINE CHANGE

**Mode:** DIAGNOSTIC ONLY — do not patch renderer, do not retry print, do not start P0.3  
**Jalali:** 1405/06/02  
**Gregorian:** 24 August 2026  
**Timezone:** Asia/Tehran (+03:30)  
**Version (unchanged):** `1405.5.27γ` / `1405.5.27.3`

```text
Product code changed: NO
P0.1 paper-form code changed: NO
NativeWindowsPrintService changed: NO
DrawInvoicePage changed: NO
Fix implemented: NO
Second print pipeline added: NO
PRINT_SUBMITTED treated as paper: NO
```

---

## Git

```text
Branch (gate, no switch/reset/rebase/merge of product):
  cursor/p0-2r-native-invoice-runtime-evidence-fa01
HEAD (gate): 82cfe15
Worktree product files: clean
Untracked only:
  deliveries/Sirman_Setup_1405.5.27γ_NATIVE_PRINT.zip
  deliveries/Sirman_Setup_1405.5.27γ_NATIVE_PRINT.sha256
  scripts/__pycache__/

This docs branch (if committed separately):
  cursor/p0-2s-windows-runtime-print-diagnostic-fa01
```

P0.2 forensic conclusions are not reinterpreted. P0.2R remains BLOCKED (no shop Windows session on that run).

---

## Absolute rule

No change to `NativeWindowsPrintService`, `DrawInvoicePage`, logo, `fa-IR`, pagination, `PaperSize`, WebView2, `IPrintService`, Output Layer, or business logic.

Diagnostics that would require altering print submit/render were **not** added.

`STOP — BLOCKED` for live capture: this agent is still Linux and has no shop `Sirman.exe` / Win32 spooler session. A shop-side **read-only** snapshot script is provided; it does not print and does not change the engine.

---

## Existing sources (preference order)

Inspected, not modified:

1. Print Center `#pc-print-result` + `ntf()` — shows `errorCode` / Persian `errorMessage`. Does **not** display `errorDetail`.
2. `host.GetPrintJob(printJobId)` → `PrintJobState.ToJson`: `status`, `errorCode`, `errorDetail`, `printJobId`, `events`.
3. `%LOCALAPPDATA%\Sirman\print\print-jobs.jsonl` — same JSON after `AppendLog`.
4. `%LOCALAPPDATA%\Sirman\print\PHASE_0_OBSERVE.log` — enqueue only.
5. Menu «تشخیص سخت‌افزار چاپ…» / `PrintHardwareDiagnostic` action `queue` — EnumJobs **only for its own** document name `SIRMAN PRINT HARDWARE TEST`, not invoice jobs.
6. Windows printer queue / PrintService Operational — not attached on this VM.

Invoice native path (`WindowsPrintHost.RunNative` → `NativeWindowsPrintService.Submit` → `PrintDocument.Print()`) sets `PRINT_SUBMITTED` after `Print()` returns without throw. It does **not** persist a Win32 spool Job ID.

---

## Test

Required: shop Windows PC, Main Printer, Test Page recently physical PASS, then **exactly one** Native Invoice print.

This agent:

```text
OS: Linux (no cupsd, no Win32 spooler)
Sirman.exe: PE32+ Windows — Exec format error
Shop Sirman session: NOT ATTACHED
```

No Native Invoice click was performed. No diagnostic DirectPrint/WebView print was started (that would be a second pipeline).

```text
Printer: EVIDENCE UNAVAILABLE
Paper: EVIDENCE UNAVAILABLE
Orientation: EVIDENCE UNAVAILABLE
Copies: EVIDENCE UNAVAILABLE
Entry: ENTRY UNKNOWN
```

---

## Application

```text
UI result: EVIDENCE UNAVAILABLE
ErrorCode: EVIDENCE UNAVAILABLE
ErrorDetail: EVIDENCE UNAVAILABLE
```

If a future shop sample is `NATIVE_PRINT_FAILED`, copy `errorCode` / `errorDetail` from `GetPrintJob` JSON **verbatim** (`Type: Message` as stored in `WindowsPrintHost` catch). Do not translate.

If `NO_DOCUMENT`: copy exact message and stop.

If `PRINT_SUBMITTED`: not physical success; inspect queue immediately.

---

## Pipeline

```text
Print(): UNKNOWN
PrintPage: UNKNOWN
Exception: EVIDENCE UNAVAILABLE
ExceptionType: EVIDENCE UNAVAILABLE
ExceptionMessage: EVIDENCE UNAVAILABLE
```

Do not infer `Print()` from `PRINT_SUBMITTED` without a shop sample.

---

## Windows

```text
Spool Job ID: NONE OBSERVED
Spool status: UNKNOWN
Queue: NOT ATTACHED
Event Viewer evidence: NOT ATTACHED
```

---

## Physical

```text
Native Test Page: PASS (prior human, not re-sampled this run)
Native Invoice: FAIL (prior human, not re-sampled this run)
This run physical sample: NOT CAPTURED
```

---

## Diagnosis

**PROVEN**

1. This cloud agent cannot execute the required one-shot shop Windows capture.
2. Product tree at gate `82cfe15` has no dirty `desktop/` or HTML.
3. Adding `PrintPage`/spool Job ID logging inside `NativeWindowsPrintService` would change product print code → **not done**.
4. Existing invoice path does not store Win32 Job ID; hardware diagnostic EnumJobs is scoped to its own test document name.
5. `errorDetail` can exist on `GetPrintJob` JSON / `print-jobs.jsonl` when `RunNative` throws; Print Center toast does not show that field.

**UNKNOWN**

All required fields of one Native Invoice attempt on the shop PC: printer, paper, orientation, copies, entry (`printInv` vs `pcDoPrint`), UI status, `errorCode`, `ErrorDetail`, `Print()` / `PrintPage`, exception, spool Job ID, queue status, PrintService event ID/message.

**Not concluded:** renderer bug, logo bug, `fa-IR` bug, pagination bug, `PRINT_SUBMITTED` = paper.

**Exact next (not a code patch):** on the shop Windows PC that already printed the Test Page, print **one** Native Invoice, then immediately run the read-only snapshot (or copy `#pc-print-result`, last `print-jobs.jsonl` line, `Get-PrintJob`, PrintService events). Do not patch `DrawInvoicePage` until that row exists.

Shop-side read-only helper (does not print, does not touch the engine):

`deliveries/Reports/P0.2S_SHOP_QUEUE_SNAPSHOT.ps1`

---

## Product

```text
Product code changed: NO
Fix implemented: NO
```

---

## Final

```text
Final status: BLOCKED
```

STOP — WAIT FOR REVIEW.
