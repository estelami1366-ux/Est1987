# SIRMAN — P0.5R8 SHOP LOGO FILE LOCATION FORENSIC

**Packet:** P0.5R8 — READ ONLY  
**Gregorian:** 31 August 2026  
**Timezone:** UTC  
**Live version (unchanged):** `1405.6.3α` / assembly `1405.6.3.1`  
**This document:** TRACE / BLOCKER ONLY. No product, print, storage, backup, rebuild, or version change.

```text
Product code changed: NO
Print changed:        NO
Postal renderer:      NO
PaperSize:            NO
Storage architecture: NO
Backup/Restore:       NO
Version changed:      NO
Rebuild:              NO
Package:              NO
Logo file moved/copied/deleted/created: NO
Settings changed:     NO
```

---

## RESULT

```text
SHOP WINDOWS EXECUTION:     NOT PERFORMED
localStorage.ll:            NOT OBSERVED
Native resolvedPath:        NOT OBSERVED
Physical logo file path:    NOT OBSERVED
File exists / size:         NOT OBSERVED
Classification of location: NOT DETERMINED
```

This packet required step 1 to run **on the actual shop Windows PC** and forbade inferring paths from the Linux agent.

This agent is a **Linux cloud VM**. It does not have:

- the running shop `Sirman.exe` / WebView2
- shop `localStorage`
- shop `%LocalAppData%\Sirman\...`
- shop File System Access folder
- shop `desktop-settings.json`

Therefore **no physical logo path is claimed**. Candidate roots from source (P0.5R7 lifecycle forensic) are **not** shop observations and are **not** repeated here as “the file is at …”.

Incoming packet text ended at section 2 (`localStorage.ll`). Remaining shop walk (Explorer checks, size, which root) was not delivered in the prompt. Work stops rather than guessing the rest.

---

## PHASE 3 CHANGE GATE

```text
Requested change: none (shop-PC forensic). Blocked: no shop Windows session.

Classification of THIS TURN: blocker report only (not A–G of logo location)

Files changed: this report only
Print / Storage / Backup / Version: NOT TOUCHED
HTML-only preserved: YES
Implementation: NONE

Gate: STOP — WAIT FOR SHOP WINDOWS EVIDENCE
```

---

## 1. Why `localStorage.ll` cannot be read from here

Brand logo reference lives in WebView2 origin storage:

```text
let logoSrc = localStorage.getItem('ll') || '';
```

(`Sirman_Final.html` — in-memory `logoSrc`, key `ll`.)

There is **no** existing `sirmanHost` method that returns `ll`. `RunBusiness` / `RunPrintHardwareDiagnostic` / `GetBackupDir` / `WriteBackupText` do not dump localStorage.

P0.5R7 history UI (`phdHistory`) shows session/printer/paper/status. It does **not** print `ll`, `logoSrc`, or a filesystem path.

P0.5R7 JSONL fields for logo (already proven from shop history **kind/reason**, not path):

```text
logoSourceKind
logoResolved
logoLoadSuccess
logoFailureReason
```

Those fields **do not** include `localStorage.ll` or `resolvedPath`.

---

## 2. Existing safe shop artifacts (not opened; do not modify)

If a later operator is **on the shop PC**, these already exist from P0.5R4/R6/R7. This agent did **not** open them.

| Artifact | What it can prove without new code |
|---|---|
| Last Native Postal `stage=LOGO` line in the P0.5R4 runtime probe log | `logoSrc=` (preview of print payload, disk refs ≤120 chars are the full string), `resolvedPath=` (native **attempted** full path even when missing), `fileExists=`, `failureReason=` |
| `%LocalAppData%\Sirman\diagnostics\history.jsonl` | kind / resolved / load / failureReason only — **not** `ll`, **not** path |
| Settings → تشخیص چاپگر → تاریخچه | same subset as JSONL; **no** path column |

Probe write (source contract, not a shop listing):

```text
NativePrintRuntimeProbe.LogPath()
  = Path.Combine(AppPaths.AppDataRoot, "print", "P0.5R4_NATIVE_RUNTIME.log")
```

`AppPaths.AppDataRoot` in source is `LocalApplicationData\Sirman`. That is where the **log file** is written. It is **not** proof of where `sirman_media\logo*` lives.

For `disk-missing`, `NativeLogoSource` still fills `ResolvedPath` with the **first attempted** combine of an existing media root + relative `disk://` path. Reading that line on shop would answer “where native looked”, not “where HTML FSA wrote”, and not `ll` unless `logoSrc=` matches `ll`.

---

## 3. What is already proven (prior packets only)

Do not treat these as this packet’s shop filesystem listing:

- A4 / A5 Native Test Page physical PASS
- A5 Native Postal physical print PASS
- Native Postal logo missing
- History: `logoSourceKind=disk`, `logoResolved=false`, `logoLoadSuccess=false`, `logoFailureReason=disk-missing`
- اطلاعات شرکت does not own/render brand logo
- Brand control: sidebar `#logo-inp` / `changeLogo`
- Reference key: `ll`
- HTML write: File System Access handle → `disk://sirman_media/...`
- Native read: `NativeLogoSource` + Desktop backup/media roots

**Not proven on shop disk this turn:** the string in `ll`, the FSA folder name, whether `logo.jpg` / `logo.png` exists anywhere, file size.

---

## 4. Required shop evidence to unblock (no code, no settings change)

Paste or attach **only** what is already on the machine:

1. Exact `localStorage.ll` from the running WebView2 (DevTools Application → Local Storage, or equivalent already-enabled inspector). Do not add a dump button in product code.
2. Last `stage=LOGO` line from the existing native runtime probe log (fields `logoSrc`, `resolvedPath`, `fileExists`, `failureReason`).
3. Explorer result **YES/NO + size** for that `resolvedPath` only. Do not search-and-copy the logo.

Until those three values are observed **on shop Windows**, location forensic is incomplete.

---

## 5. FINAL

```text
Product code changed: NO
Print changed:        NO
Storage architecture changed: NO
Backup changed:       NO
Version changed:      NO

localStorage.ll:         NOT OBSERVED
Physical logo path:      NOT OBSERVED
This agent is not the shop Windows PC.

STOP — WAIT FOR REVIEW.
STOP — WAIT FOR SHOP WINDOWS EVIDENCE.
```
