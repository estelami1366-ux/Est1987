# SIRMAN — ARCH-26 Copy-only Recovery Acceptance Report

**Date:** 2026-09-05  
**Packet:** ARCH-26 Recovery Acceptance Harness (RAH)  
**Product version:** `1405.6.3α` (unchanged)  
**Branch:** `cursor/arch-26-recovery-acceptance-fa01`  
**Base:** `cursor/arch-25-phonebook-restore-contract-fa01`  
**Kind:** copy-only synthetic recovery acceptance  
**Shop:** not shop-VERIFIED  
**Final status:** **COMPLETED — BACKUP / RECOVERY ACCEPTANCE PASSED**

This packet is **not** new architecture. Production Backup/Restore/Phonebook/attachments/checksum/SQLite/Print were not redesigned. Restore apply remains HTML Merge/Replace. Core RestorePlan remains `Applied = false`. ARCH-27 was **not** started.

Do NOT start ARCH-27.

---

## 1. Executive verdict

Copy-only synthetic Recovery Acceptance (RAH) executed the ARCH-24 A–S blocking checklist against an isolated temporary environment.

**BACKUP / RECOVERY = COMPLETE**

All blocking criteria A–S **PASS**. Sidecar-absent restore is classified **PARTIAL WITH EXPLICIT MEDIA FAILURE**, not FULL RECOVERY. No live shop data was used.

---

## 2. RAH environment

| Item | Value |
|---|---|
| Source app dir | isolated `os.tmpdir()/sirman-arch26-rah-*/source-app` |
| Portable package | sibling `portable/` (JSON + `sirman_media/`) |
| Destination #1 | empty RAM + empty LS + copied sidecar tree |
| Destination #2 | second empty RAM/LS + copied sidecar tree (no source-app path) |
| Missing-sidecar dest | JSON only |
| Shop `lb` / real backup | FORBIDDEN — not used |
| Apply engine | production `applyBackupReplaceSections` / `applyBackupMergeSections` |
| Assemble | production `_buildFullBackupData` |
| Finalize | production `finalizeBackupPackage` + SHA-256 attach (Node `crypto` as `subtle` stand-in) |
| Failure injection | test-only wrap in RAH; no production hook |

---

## 3. Golden source dataset

Synthetic structurally representative SIRMAN RAM, frozen before backup.

| Collection | Count |
|---|---|
| invoices | 1 |
| sales | 1 |
| warranties | 1 |
| parts | 1 |
| accounts | 1 |
| products / inventory / services / svcs / tasks | 1 each |
| defectiveStock / warehouseDocs / stockMoves / warehouses | 1 each |
| daqi / daqiWarehouse / daqiVouchers / postalHistory | 1 each |
| phonebook | 8 |
| userRoles | 1 |
| counters | invCtr=102, invoiceUidCtr=101, saleCtr=2, saleUidCtr=101 |

Identities: invoice `INV-RAH-1`, sale `SL-0001`, warranty `W-RAH-1`.

Phonebook rows: normal; exact empty-phone clone ×2; distinct empty; missing phones; null phones; Persian/Arabic; unknown extra field.

Attachments: inline `docs[]` on invoice+warranty; `disk://sirman_media/docs/rah_sidecar.bin` on sale; actual sidecar bytes.

Media SHA-256: `e51dedfeb3dadb8f5f7882b766c548aefc590b92a00c32348b2547c3cbe626c0`

Backup canonical digest: `3a793777b2dff06221d3dbfbce72565d1cd33963c176e2cc836d0387ea9a42ce`

---

## 4. Backup artifact details

Generated by `_buildFullBackupData` + `finalizeBackupPackage` + SHA-256.

| Field | Result |
|---|---|
| magic | `SIRMAN_BACKUP` |
| schemaVersion | 1 |
| version | `1405.6.3α` |
| required collections | present |
| optional collections | present |
| settings | present (`company`, `printSettings`, …) |
| phonebook | present (8) |
| loginPw / userRoles / senderInfo / logoSrc / acH | present |
| attachmentsIndex | present (3 entries: 2 inline + 1 disk) |
| manifest | present |
| itemCounts | present |
| checksumAlgo | SHA-256 |
| checksum == canonical SHA | YES |
| pretty disk SHA-256 | `8acc86b290c4b0a494faed330d686a392f7a1ba0b1ff1cb082900ba1a75187b2` (not the hash input) |

---

## 5. Portable package details

Copied JSON + `sirman_media/` to a directory **outside** the source-app tree.

| Check | Result |
|---|---|
| source JSON SHA-256 | `8acc86b290c4b0a494faed330d686a392f7a1ba0b1ff1cb082900ba1a75187b2` |
| copied JSON SHA-256 | identical |
| sidecar `docs/rah_sidecar.bin` | 49 bytes, SHA-256 identical |
| depends on source-app | no |

---

## 6. Clean destination #1

Empty RAM (`phonebook=[]`, `invoices=[]`) and empty LS (`length=0`) before Replace. No source Phonebook. No business records. Sidecar tree copied from the portable package into an isolated dest media root.

---

## 7. Replace results

Production `applyBackupReplaceSections(d, null)` after the production import gates (required / structural / portable / checksum).

Gate: **ok**. Required, optional, settings, Phonebook, security/ops, attachment refs, inline data, disk ref, itemCounts, sections, manifest all restored into dest RAM.

Post-restore LS: `sv` / `svParts` / `svWarr` / `svSales` / `svTasks` / `svAccounts` / `svDaqi*` / `svWarehouses` / `svRoles` wrote their keys. See §22 for `warehouseDocs` / `stockMoves` LS (RAM restored; those two persist helpers are not called by production Replace).

---

## 8. Source-vs-restored comparison

Frozen source RAM vs dest #1 RAM after Replace.

Every required and optional collection: source count = restored count; missing=[], extra=[], changed=[], no identity/order diffs.

Declared derived exclusions only: `exportedAt`, `checksum` / `checksumAlgo`, `manifest.exportedAt`.

Re-export digest (those keys stripped): **identical**  
`e078240e13c782df1e6d96102b5e34fc99e7982db236b69465f0a2f2906d2127`

Audit logs compared as payload (best-effort per ARCH-24). No undeclared RAM differences.

---

## 9. Phonebook results

| Proof | Result |
|---|---|
| 1 normal contacts preserved | PASS |
| 2 exact duplicate empty-phone do not multiply on Replace (both copies kept) | PASS (2) |
| 3 distinct empty-phone remain distinct | PASS |
| 4 missing/null phones preserved | PASS |
| 5 Persian/Arabic preserved | PASS (`محمد` / `العربية` / `۰۹۱۲۳۳۳۳۳۳۳`) |
| 6 unknown fields preserved | PASS (`xyz:42`, `nested.k`) |
| 7 order preserved | PASS |

Replace A–E (selected Phonebook on an otherwise valid package):

| Case | Payload | Result |
|---|---|---|
| A | `phonebook: [records]` | replaces (`X`) |
| B | `phonebook: []` | clears (`[]`) |
| C | key missing | KEEP LIVE (`A`,`B`) |
| D | `null` | KEEP LIVE |
| E | wrong type `{}` | KEEP LIVE |

---

## 10. Merge idempotency

Second Merge of the **same** portable backup on dest #1:

- Phonebook did not grow (exact empty clones stayed 2; distinct empty remained)
- ID-based collections did not grow

Synthetic 530 empty-phone replay (not a shop dataset):

`530 → 530 → 530 → 530 → 530`

---

## 11. Attachment / media results

| Check | Result |
|---|---|
| inline parent `docs[]` data survives | PASS |
| `disk://` reference survives | PASS |
| sidecar file exists in dest media tree | PASS |
| sidecar SHA-256 before vs after | identical |
| `resolveDiskRef` against isolated dest | PASS (`blob:rah:…`) |

A disk-backed attachment **can** be restored from JSON + sidecar. Not metadata-only.

---

## 12. Referential integrity

| Reference | Result |
|---|---|
| warranty `docs` parentId → warranty.id | PASS |
| sale `docs` parentId → sale.id | PASS |
| invoice `docs` parentId → invoice.id | PASS |
| defectiveStock.warrantyId / invoiceId | PASS |
| warehouseDocs.toWh → warehouses.id | PASS |
| stockMoves.refDoc / whId | PASS |
| task.link warranty id | PASS |
| daqi.warrantyId / daqiVouchers.daqiId | PASS |
| daqi.agencyPhonebookIdx same logical contact | PASS (idx 0 = علی) |

No invented mappings. No orphans.

---

## 13. Checksum corruption rejection

Copy of valid backup; one payload field changed (`invoices[0].seller`); checksum not regenerated.

Restore gate: **REJECT** (portable section hash and/or SHA-256). Destination Phonebook unchanged (`LIVE`).

Stripping `sectionChecksums` and tampering still **REJECT** at the SHA-256 compare stage.

---

## 14. Required-section rejection

Separate copies with one required key removed: warranties, invoices, sales, parts, accounts.

Each: **fail-closed**, not applied, not filled with `[]`.

---

## 15. Malformed-section rejection

`warranties={}`, `invoices=""`, `sales=null`, `parts={}`, `accounts=123`.

Each: **fail-closed before destructive apply**.

---

## 16. Rollback

Known pre-restore state (including truthy loginPw/logoSrc/counters). Test-only inject throw **after** `applyBackupReplaceSections` inside production `applyBackupSelective`. Rollback Replace(safety) ran.

Pre-state vs post-rollback: **no undeclared RAM differences**. Phonebook `قبل` and invoice `PRE-INV` restored.

No permanent production failure hook.

---

## 17. Clean-install #2

Second empty destination restored from the **portable** package only. Source-app path not referenced.

Dest #2 vs frozen source: no undeclared diffs. Dest #2 vs dest #1 (pre-merge snapshot): identical.

---

## 18. Sidecar failure result

Valid JSON + missing media tree.

JSON Restore succeeds (`disk://` string survives). `resolveDiskRef` returns empty.

**Classification: PARTIAL WITH EXPLICIT MEDIA FAILURE**  
Not FULL RECOVERY. Current contract does not fail-closed the JSON apply when the sidecar is absent; ARCH-26 did not redesign that.

---

## 19. Full regression totals

Filled after the official suite run in this packet.

| Suite | Result |
|---|---|
| `node test_laegh.js Sirman_Final.html` | PENDING official record |
| `/home/ubuntu/.dotnet/dotnet test desktop/Sirman.Core.Tests` | PENDING official record |

---

## 20. RAH test totals

| Area | Tests (HTML group ARCH-26) |
|---|---|
| SHA locks | 1 |
| freeze / backup / portable / empty dest | 4 |
| Replace + source-vs-restored | 1 |
| Phonebook | 1 |
| Merge + 530 | 1 |
| Replace A–E | 1 |
| attachment/media | 1 |
| references | 1 |
| checksum corruption | 1 |
| required-section | 1 |
| malformed-section | 1 |
| rollback | 1 |
| clean-install #2 | 1 |
| sidecar failure | 1 |
| digest | 1 |
| wording | 1 |
| criteria A–S | 1 |
| **RAH HTML total** | **20** |

Core: 6 ARCH-26 lock facts (SHA, synthetic fixture, RestorePlan, Host, Print/SQLite, report).

---

## 21. ARCH-24 criteria A–S

| ID | Criterion | Result |
|---|---|---|
| A | Backup generation | **PASS** |
| B | Integrity | **PASS** |
| C | Portability | **PASS** |
| D | Required collections fail-closed | **PASS** |
| E | Optional collections after restore | **PASS** (RAM) |
| F | Phonebook | **PASS** |
| G | Attachments/media | **PASS** (sidecar present) |
| H | Restore Replace | **PASS** |
| I | Restore Merge | **PASS** |
| J | Migration | **PASS** (schema 0 gate + migrateBackup; ARCH-3 goldens remain) |
| K | Referential integrity | **PASS** |
| L | Counts | **PASS** |
| M | Digest comparison | **PASS** |
| N | No unexpected loss | **PASS** (RAM; audit best-effort) |
| O | No duplicate growth | **PASS** |
| P | Idempotent repeat restore | **PASS** |
| Q | Rollback | **PASS** |
| R | Clean install | **PASS** |
| S | End-to-end comparison | **PASS** |

---

## 22. Explicit exceptions

Unchanged from ARCH-24 §13 (must stay explicit):

- not shop-VERIFIED
- pretty-print disk bytes are not the hash input
- `crypto.subtle` unavailable → checksum may skip (legacy HTML-only; RAH used Node SHA-256)
- IndexedDB mirror is extra
- audit logs best-effort
- Core RestorePlan does not apply; HTML remains the apply engine
- Phonebook has no stable id; daqi remains positional
- external media bytes are not inside the JSON file
- missing sidecar + valid JSON = PARTIAL WITH EXPLICIT MEDIA FAILURE, not FULL RECOVERY

**POST-CLOSURE OPTIONAL IMPROVEMENT** (does not reopen A–S; not patched in ARCH-26): production Replace/Merge assign `warehouseDocs` and `stockMoves` in RAM but do not call `svWarehouse` / `svStockMoves`. A later page reload hydrates those two collections from LS and would drop them unless another UI save runs. RAM SoT after restore matched the golden source. Do not treat this as a silent FULL RECOVERY of those two LS keys.

---

## 23. Data-loss analysis

- Required + optional + Phonebook + settings + ops/security in JSON: round-tripped.
- Exact empty-phone clones: Replace keeps both; Merge does not grow.
- Missing/null/wrong-type Phonebook Replace: does not wipe live.
- Tampered checksum / missing required / malformed required: dest not mutated.
- Rollback: pre-state restored.
- Inline attachments: bytes in JSON survived.
- Disk attachments: recovered when the sidecar was copied; **not** recovered when the sidecar was omitted (explicit partial).

No undeclared RAM data loss on the copy-only path.

---

## 24. Definition-of-Done evaluation (ARCH-24 §12)

1. Required backup data in JSON — **yes**  
2. Integrity proven — **yes**  
3. Restore safe (required missing fails; Phonebook Merge no clone-growth; missing Phonebook does not wipe) — **yes**  
4. Known corruption rejected — **yes**  
5. External media explicit and recoverable by copying the folder — **yes**  
6. Repeat Merge does not grow exact duplicates — **yes**  
7. Clean-install restore matches frozen source — **yes**  
8. Rollback demonstrated — **yes**  
9. ARCH-26 E2E report PASS — **yes**

---

## 25. FINAL VERDICT

**COMPLETED — BACKUP / RECOVERY ACCEPTANCE PASSED**

This is copy-only synthetic recovery acceptance. It is not shop-VERIFIED.

Do NOT start ARCH-27. The Backup/Recovery program ends. Future work is POST-CLOSURE OPTIONAL IMPROVEMENT.

BACKUP / RECOVERY = COMPLETE
