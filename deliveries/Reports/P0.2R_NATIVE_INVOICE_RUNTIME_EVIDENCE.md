# SIRMAN — P0.2R NATIVE INVOICE RUNTIME EVIDENCE
## Evidence capture only — no product change

**Mode:** CAPTURE ONLY — do not patch, do not retry print, do not instrument  
**Jalali:** 1405/06/02  
**Gregorian:** 24 August 2026  
**Timezone:** Asia/Tehran (+03:30)  
**Version (unchanged):** `1405.5.27γ` / `1405.5.27.3`

```text
Branch: cursor/p0-2r-native-invoice-runtime-evidence-fa01
HEAD parent (git gate): 891a16a  on cursor/p0-2-native-invoice-forensic-fa01
P0.1 checkpoint: 56832e5
P0.2 forensic: deliveries/Reports/P0.2_NATIVE_INVOICE_FORENSIC.md
               (conclusions not reinterpreted)

Product code changed: NO
P0.1 paper-form code changed: NO
P0.2 renderer code changed: NO
Fix implemented: NO
Instrumentation added: NO
PRINT_SUBMITTED treated as paper: NO
```

---

## Git gate (before this docs file)

Recorded on the Linux agent, worktree product files clean:

```text
git status --short
  ?? deliveries/Sirman_Setup_1405.5.27γ_NATIVE_PRINT.sha256
  ?? deliveries/Sirman_Setup_1405.5.27γ_NATIVE_PRINT.zip
  ?? scripts/__pycache__/

git branch --show-current
  cursor/p0-2-native-invoice-forensic-fa01

git rev-parse --short HEAD
  891a16a
```

Untracked items are the existing shop zip / sha256 / pycache only. No product file was dirty. This P0.2R file is documentation only.

---

## Hard stop

This run did **not**:

- modify `NativeWindowsPrintService`, `DrawInvoicePage`, logo handling, `fa-IR`, pagination, `PaperSize`, WebView2, `IPrintService`, or Output Layer
- add retries, workarounds, or instrumentation
- perform a print attempt (none is possible on this host)

If capturing `ErrorDetail` required new product logging: **STOP — BLOCKED** (no instrumentation was added).

---

## P0.2 forensic (not replaced)

From `deliveries/Reports/P0.2_NATIVE_INVOICE_FORENSIC.md`, left as-is:

- Native Test Page: physical **PASS**
- Native Invoice: physical **FAIL**
- First source split after shared `Submit` / P0.1 `ApplyPaperSize`: `PrintPage` → `DrawInvoicePage` + `TryLogo`
- Exact Windows mode (PrintPage throw vs post-`Print()` spool drop vs hung `HasMorePages`) was **NOT OBSERVED**

P0.2R does not change that source conclusion. It was supposed to supply the missing Windows runtime row. That row was **not captured**.

---

## ONE TEST — not executed

Required: real Windows shop machine, same Main Printer, exactly one Native Invoice attempt after Test Page physical PASS.

This agent:

```text
OS: Linux cursor 6.12.94+ x86_64
wmic / Get-Printer / Win32 spooler: NOT PRESENT
Sirman.exe shop session: NOT ATTACHED
Windows Event Viewer / PrintService Operational: NOT ATTACHED
printer queue: NOT ATTACHED
AppData print-jobs.jsonl (shop): NOT ATTACHED
PHASE_0_OBSERVE.log (shop): NOT ATTACHED
```

No Native Invoice click was made. No automatic retry.

---

## Required evidence (this capture)

```text
Date: 24 August 2026
Time: capture not performed on shop Windows
Timezone: Asia/Tehran (+03:30) — agent clock UTC; shop attempt N/A

Branch: cursor/p0-2-native-invoice-forensic-fa01 (gate) / cursor/p0-2r-native-invoice-runtime-evidence-fa01 (this docs)
HEAD: 891a16a (gate; product tree)

Printer:
PrinterName: EVIDENCE UNAVAILABLE

Paper:
Paper: EVIDENCE UNAVAILABLE
Orientation: EVIDENCE UNAVAILABLE
Copies: EVIDENCE UNAVAILABLE

Entry:
ENTRY UNKNOWN

UI result:
EVIDENCE UNAVAILABLE
  (not PRINT_SUBMITTED / NATIVE_PRINT_FAILED / NO_DOCUMENT / other toast — none observed)

ErrorCode:
errorCode = EVIDENCE UNAVAILABLE

ErrorDetail:
ErrorDetail = EVIDENCE UNAVAILABLE

PRINT() status:
UNKNOWN

PrintPage status:
UNKNOWN

Spool Job ID:
Job ID: NONE OBSERVED

Spool status:
UNKNOWN

Physical result:
NOT CAPTURED THIS RUN
  (prior human round remains: Test Page PASS / Invoice FAIL — not re-sampled here)
```

---

## Existing mechanisms inspected (read-only, no new probes)

These already exist. None of them yielded a shop sample in this run, because the shop AppData / UI / spooler are not on this VM.

| Mechanism | What it can hold | Observed this run |
|---|---|---|
| Print Center box `#pc-print-result` + `ntf()` | Persian message; `errorCode` in parentheses on failure. Treats `status==='PRINT_SUBMITTED'` as UI success. Does **not** print `errorDetail` (`Sirman_Final.html` `printEngineApplyResult` copies `errorCode` / `errorMessage` only; **zero** `errorDetail` references in that HTML) | EVIDENCE UNAVAILABLE |
| `host.GetPrintJob(printJobId)` JSON | `status`, `errorCode`, `errorDetail`, `printJobId`, `events` (`PrintJobState.ToJson`) | EVIDENCE UNAVAILABLE — no live host |
| `%AppData%\…\print\print-jobs.jsonl` | same JSON lines after `AppendLog` | EVIDENCE UNAVAILABLE — shop file not here |
| Menu «باز کردن پوشه لاگ چاپ» | opens that print folder | not operable here |
| `PHASE_0_OBSERVE.log` | enqueue stage + truncated JSON (`PrintPhase0Observer`) | EVIDENCE UNAVAILABLE |
| `PRINT_DIAGNOSTIC.log` | hardware diagnostic, not this invoice click | not used; no retry |
| Windows PrintService Operational / printer queue | Win32 Job ID / job status | EVIDENCE UNAVAILABLE |

`WindowsPrintHost.RunNative` catch already stores:

```text
errorCode = NATIVE_PRINT_FAILED
ErrorDetail = ex.GetType().Name + ": " + ex.Message
```

That string is **not** in this packet. Source existence is not a captured exception.

`PRINT_SUBMITTED` is set only after `PrintDocument.Print()` returns without throw. Per P0.2R rules it is **not** used to prove `PRINT()` / `PrintPage` / paper.

---

## PRINT() / PrintPage / Spool (evidence-only)

```text
PRINT() REACHED | PRINT() NOT REACHED | UNKNOWN
→ UNKNOWN

PrintPage: ENTERED | THREW | COMPLETED | UNKNOWN
→ UNKNOWN
Exception type/message: EVIDENCE UNAVAILABLE

Spool: JOB CREATED | JOB NOT CREATED | JOB ERROR | JOB COMPLETED | UNKNOWN
→ UNKNOWN
Job ID: NONE OBSERVED
```

Do not infer `PRINT()` from a missing or present `PRINT_SUBMITTED`. None was observed this run.

---

## Interpretation (rules honored)

Not concluded:

- `PRINT_SUBMITTED` = physical success
- no paper = renderer bug
- logo bug / `fa-IR` bug / pagination bug

P0.2 still stands as **source split**, not as a proven Windows failure mode.

---

## What is proven

1. This agent cannot execute the required one-shot shop capture (Linux, no spooler, no `Sirman.exe` session).
2. Product tree at gate `891a16a` is unchanged (no dirty `desktop/` or HTML).
3. P0.2 forensic file is unchanged in meaning: invoice-only split is `DrawInvoicePage` / `TryLogo`; Windows mode unproven.
4. Existing host JSON **can** carry `errorDetail` as `Type: Message` if `RunNative` throws; Print Center toast **does not** display that field. That is a source observation about **where** evidence would appear, not a diagnosis of the shop FAIL.
5. Required runtime fields for this task are **EVIDENCE UNAVAILABLE**.

## What remains unknown

Printer name, paper, orientation, copies, entry (`printInv` vs `pcDoPrint`), UI status, `errorCode`, `ErrorDetail`, whether `Print()` ran, whether `PrintPage` entered/threw/completed, spool Job ID, spool job fate, physical result of a new one-shot click.

## Exact next component to investigate

Not a code patch.

Next is **shop Windows, one Native Invoice click** (same Main Printer that already printed the Test Page), then read **without new instrumentation**:

1. Exact toast / `#pc-print-result` text  
2. `GetPrintJob` JSON: `status`, `errorCode`, `errorDetail`  
3. `%AppData%\…\print\print-jobs.jsonl` last invoice line  
4. Printer queue Job ID + status immediately after the click  

Until those four exist, do not patch `DrawInvoicePage`.

---

## Compact block

```text
P0.2R NATIVE INVOICE RUNTIME EVIDENCE

Branch: cursor/p0-2r-native-invoice-runtime-evidence-fa01
HEAD: (this docs commit; gate parent 891a16a)

Printer: EVIDENCE UNAVAILABLE
Paper: EVIDENCE UNAVAILABLE
Orientation: EVIDENCE UNAVAILABLE
Copies: EVIDENCE UNAVAILABLE

Entry: ENTRY UNKNOWN
UI result: EVIDENCE UNAVAILABLE
ErrorCode: EVIDENCE UNAVAILABLE
ErrorDetail: EVIDENCE UNAVAILABLE

PRINT(): UNKNOWN
PrintPage: UNKNOWN

Spool Job: NONE OBSERVED
Spool status: UNKNOWN

Physical result: NOT CAPTURED THIS RUN

Proven:
  Linux agent cannot reach shop spooler/UI.
  Product tree clean at 891a16a.
  P0.2 source split unchanged; Windows mode still unproven.
  Host JSON can hold errorDetail; Print Center UI does not show it.
Unknown:
  All required shop runtime fields for one invoice attempt.

Product code changed:
NO

Fix implemented:
NO

Report:
deliveries/Reports/P0.2R_NATIVE_INVOICE_RUNTIME_EVIDENCE.md

Final status:
BLOCKED
```

STOP — WAIT FOR REVIEW.
