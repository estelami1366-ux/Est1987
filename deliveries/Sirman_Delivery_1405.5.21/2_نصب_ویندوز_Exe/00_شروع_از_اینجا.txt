پکیج نصب ویندوزی سیرمان
=======================
1) build-win.bat
2) install-sirman.bat   (یا نصب_سیرمان.bat)

فقط از install-sirman.bat استفاده کنید.
دیگر به install-package.ps1 نیاز نیست.

نصب‌کننده مسیر را می‌پرسد و فقط آنجا کپی می‌کند.
